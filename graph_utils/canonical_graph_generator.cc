
#include "graph.h"

namespace graph_utils {

explicit CanonicalGraphGenerator::CanonicalGraphGenerator(const int n) {
  // TODO(radkokotev) provide implementation.
}

void CanonicalGraphGenerator::GenerateUpperObjects(
    const Graph &g, vector<Graph *> *upper_obj) {
  // TODO(radkokotev) provide implementation.
}

void CanonicalGraphGenerator::GenerateLowerObjects(
    const Graph &g, vector<Graph *> *lower_obj) {
  // TODO(radkokotev) provide implementation.
}

void GetAllRelatedLowerObjects(const Graph &g,
                               vector<Graph *> *lower_obj) {
  // TODO(radkokotev) provide implementation.
}

void FindGraphsFromLowerObject(const Graph &lower_obj,
                               vector<Graph *> *graphs) {
  // TODO(radkokotev) provide implementation.
}

} // namespace graph_utils
